{
  "name": "tap-logging-system",
  "version": "1.0.0",
  "description": "System for tracking NFC card taps and logging to Google Sheets",
  "main": "api/log.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "googleapis": "^118.0.0"
  },
  "devDependencies": {
    "vercel": "^29.1.1"
  }
}
